# career-buddy
A mental-health-aware job + scholarship app for college students. Uses React Native via Expo (iOS + Android), Expo Router for file-based navigation, Supabase — auth, postgres database, file storage, Zustand — global state with AsyncStorage persistence, TanStack Query — server state + caching, AI matching, day plans, prep feedback EAS — builds
